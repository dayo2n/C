#include <stdio.h>
//#include <conio.h>
//원래는 있어야된다는데 ... 내장함수 getchar(), getche(), getch()의 전처리기
//맥에서는 지원 안되니까 이론 짱짱하게 보기

int main(void)
{

    char ch;

    // printf("문자를 계속 입력하고 Enter를 누르면 다음 줄에서 그대로 출력 >> \n");
    // while ((ch = getchar()) != 'q')
    //     putchar(ch);

    // printf("\n문자를 누를때마다 출력 ex)java->jjaavvaa >>\n");
    // while ((ch = getche()) != 'q')
    //     putchar(ch);

    // printf("\n문자를 누르면 한 번 출력 : 그냥 한번만 보인다 >>\n");
    // while ((ch = getch()) != 'q')
    //     putch(ch);
    // printf("\n");

    return 0;
}